pipeline {
   agent any

   stages {
      stage('Build') {
         steps {
            // Install libraries
            sh '''
                   echo 'Hello World'
                '''
         }
      }
   }
}
